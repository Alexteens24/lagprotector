{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Gradle: eclipse (refresh IDE classpath)",
      "type": "shell",
      "command": "./gradlew eclipse",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "build",
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}
